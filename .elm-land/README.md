# The ".elm-land" folder
> "What is all this stuff?"

This folder will be removed and ignored from Git once the
`elm-land` NPM package is officially published.

- I am vendoring the WIP tool in the `package` folder
- I am keeping the src/ElmLand packages around until the tool generates them on start